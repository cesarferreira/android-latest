![Image](https://github.com/cesarferreira/android-latest/raw/master/extras/web-screenshot.png)

Latest android versions for developers. Can be found online here [cesarferreira.com/android-latest](http://cesarferreira.com/android-latest/)


------------
The following table was automatically generated based on [data.json](data.json):

<center>

| Key | Value     | Sample      |
|:-----------:|:--------:|:--------:|
| androidGradlePluginVersion | <%= hash['androidGradlePluginVersion'] %> | classpath 'com.android.tools.build:gradle:<strong><%= hash['androidGradlePluginVersion'] %>'</strong> |
| gradleVersion | <%= hash['gradleVersion'] %> | https://services.gradle.org/distributions/gradle-<strong><%= hash['gradleVersion'] %></strong>-all.zip |
| latestAndroidVersionCode | <%= hash['latestAndroidVersionCode'] %> | N/R |
| compileSdkVersion | <%= hash['compileSdkVersion'] %> | compileSdkVersion <strong><%= hash['compileSdkVersion'] %></strong> |
| buildToolsVersion | <%= hash['buildToolsVersion'] %> | buildToolsVersion "<strong><%= hash['buildToolsVersion'] %></strong>" |
| targetSdkVersion | <%= hash['targetSdkVersion'] %> | targetSdkVersion <strong><%= hash['targetSdkVersion'] %></strong> |
| latestSupportVersion | <%= hash['latestSupportVersion'] %> | compile 'com.android.support:support-v7:<strong><%= hash['latestSupportVersion'] %></strong>' |
| googlePlayServicesVersion | <%= hash['googlePlayServicesVersion'] %> | compile 'com.google.android.gms:play-services:<strong><%= hash['googlePlayServicesVersion'] %></strong>' |
| latestAndroidVersionNumber | <%= hash['latestAndroidVersionNumber'] %> | N/R |
| latestAndroidVersionName | <%= hash['latestAndroidVersionName'] %> | N/R |
| latestAndroidReleaseDate | <%= hash['latestAndroidReleaseDate'] %> | N/R |

</center>

## Motivation

I'm always wondering what the latest versions of buildTools/support/etc. are, so here is a repository with the updated information for consultation.
Anyone can open a pull request if any version comes out and I fail to notice.

## Available outputs

- [data.json](data.json)
- [build.gradle](generated/build.gradle)
- [index.html](http://cesarferreira.com/android-latest/)
- [README.md](generated/README.md)

## Using the `build.gradle`

Apply this in your main `build.gradle`
```groovy
apply from: 'https://raw.githubusercontent.com/cesarferreira/android-latest/master/generated/build.gradle'
```

Use the variables at will
```groovy
buildToolsVersion rootProject.ext.buildToolsVersion
```

## Installation

Just run
> `gem install erubis json colorize`

and you are free to execute the generator!


## Contributing

1. Fork the project
2. Update the `data.json`
3. Run the `ruby generator.rb`
4. Create the pull request

## License

This project is available under the MIT license. See the LICENSE file for more info.
